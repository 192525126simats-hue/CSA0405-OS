#include<stdio.h>
#include<fcntl.h>
#include<unistd.h>
int main{
int ftd;
char msg[]="hello";
char buf10];
fd=open("file.txt",O_CREATE|O_RDWR,0644);
1seek(fd,0,SEEK_SET);
read(fd,buf,5);
buf[5]='\0';
printf("Data :%s\n",buf);
close(fd);
return 0;
}
