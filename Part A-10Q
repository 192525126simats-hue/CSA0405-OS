#include <stdio.h>
#include <sys/stat.h>
#include <time.h>
int main()
{
struct stat s;
stat("student.txt", &s);
printf("File Size: %ld bytes\n", s.st_size);
printf("Permissions: %o\n", s.st_mode & 0777);
printf("Last Modified: %s", ctime(&s.st_mtime));
return 0;
}
