#include <stdio.h>
#include <fcntl.h>
#include <unistd.h>
int main()
{
int fd;
char buf[100];
int n;
fd = open("student.txt", O_RDONLY);
n = read(fd, buf, 100);
buf[n] = '\0';
printf("File Content:\n%s", buf);
close(fd);
return 0;
}
